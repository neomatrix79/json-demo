export interface UserType {
  id: number;
  name: string;
  password: string;
}

export interface AlbumType {
  albumId: number;
  id: number;
  title: string;
  url: string;
  thumbnailUrl: string;
}
